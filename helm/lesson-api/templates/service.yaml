apiVersion: v1
kind: Service
metadata:
  name: "{{ include "lessonapi.name" . }}"
  labels:
{{ include "lessonapi.labels" . | indent 4 }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.service.port }}
  selector:
{{ include "lessonapi.matchLabels" . | indent 4 }}